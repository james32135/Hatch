{
  "name": "@hatch/web",
  "version": "0.1.0",
  "private": true,
  "description": "HATCH frontend — Phase 10 LAST. Do not build until backend is production-ready.",
  "type": "module",
  "scripts": {
    "dev": "echo Phase 10 not started",
    "build": "echo Phase 10 not started"
  }
}
